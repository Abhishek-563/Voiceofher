# MongoDB Atlas connection string
# Replace <username>, <password>, and <dbname> with your own values.
# If your password contains special characters like $, @, or %, URL-encode them.

MONGO_URI=mongodb+srv://<username>:<password>@cluster0.f6mk8nx.mongodb.net/<dbname>?retryWrites=true&w=majority

JWT_SECRET=your_jwt_secret_here
PORT=5000
FAST2SMS_API_KEY=YOUR_API_KEY_HERE
